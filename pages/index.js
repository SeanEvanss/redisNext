import CarForm from "../lib/CarForm";

export default function Home() {
  return (
    <div>
      <h1>
        Create a car ? (will not be shipped to you)
      </h1>
      <CarForm />
    </div>
  )
}
